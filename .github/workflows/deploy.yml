name: Pipeline
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Tests:
    runs-on: ubuntu-latest
    steps:
      - run: npm run test
  Lint:
    runs-on: ubuntu-latest
    steps:
      - run: npm run lint
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: npm run build
